llm_config:
  # The API key for the LLM API
  api_key: "sk-zcpmjwctlnkthagcorztornyziblyxvkjireovscqsymjqyn"
  # The URL of the LLM API
  base_url: "https://api.siliconflow.cn/v1"
  # model
  model: "deepseek-ai/DeepSeek-V3"

  temperature: 0.
  max_tokens: 100


agent_config:
  # Agent行为控制
  verbose: true        # 是否显示内部调试信息
  intermediate_steps: false  # 是否返回中间推理步骤
  error_handling: true # 是否启用错误处理机制

